<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:tx="http://www.springframework.org/schema/tx" xmlns:jdbc="http://www.springframework.org/schema/jdbc"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://www.springframework.org/schema/tx
       http://www.springframework.org/schema/tx/spring-tx.xsd
       http://www.springframework.org/schema/jdbc
       http://www.springframework.org/schema/jdbc/spring-jdbc.xsd">

    <!--This enables Spring’s support for transaction management using the @Transactional annotation
    @Configuration
    @EnableTransactionManagement
    public class AppConfig {
     // ...
    }-->
    <tx:annotation-driven transaction-manager="txManager"/>


    <bean id="txManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
        <property name="dataSource" ref="productionDataSource"/>
        <property name="rollbackOnCommitFailure" value="true"/>

    </bean>

    <bean class="hsl.devspace.app.admin.ExceptionResolver"/>

    <jdbc:embedded-database id="productionDataSource"/>
</beans>
